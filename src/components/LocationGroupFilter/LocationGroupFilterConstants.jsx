/**
 * The max dropdown height to display
 */
export const MAX_DROPDOWN_HEIGHT = 400;

/**
 * The max item count to display in the dropdown
 */
export const MAX_ITEM_COUNT = 10;

/**
 * The max height for each row item in the dropdown
 */
export const ROW_HEIGHT = 45;

/**
 * The max number of rows to overscan in virtualize list
 * Recommended value is <=10 before performance degrades
 */
export const OVERSCAN_ROW_COUNT = 10;
